[package]
name = "beacon-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
beacon-parser = { path = "../parser" }
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
tree-sitter = "0.25.10"

[dev-dependencies]
tempfile = "3.8"
